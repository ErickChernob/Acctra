# public_html_ready.zip is a single ~161MB archive of public_html_ready/ -
# it exceeds both this chat's file-delivery limit and GitHub's 100MB
# single-file push limit, so only the unpacked folder is tracked here.
# Regenerate the zip locally after cloning with:
#   cd public_html_ready && zip -r -X ../public_html_ready.zip .
/public_html_ready.zip
